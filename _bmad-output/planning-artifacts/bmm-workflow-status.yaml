# Workflow Status Template

# This tracks progress through BMM methodology Analysis, Planning, and Solutioning phases.
# Implementation phase is tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2026-01-02"
project: "Pay-Up Legends"
project_type: "game"
user_description: "Battle royale game with micro-transactions. Developer is relatively new to game dev but has worked in Unity and Unreal."
selected_track: "gamedev"
field_type: "greenfield"
workflow_path: "_bmad/bmgd/workflows/workflow-status/paths/gamedev-greenfield.yaml"

# Workflow Status Tracking
workflow_status:
  # Phase 0: Pre-production (Optional - User Selected)
  brainstorm-game: "required"
  game-brief: "required"

  # Phase 1: Design (Required)
  gdd: "required"
  narrative: "conditional"  # Determined after GDD based on narrative focus

  # Phase 2: Technical (Required)
  game-architecture: "required"
  test-framework: "recommended"

  # Phase 3: Production (Required)
  sprint-planning: "required"
